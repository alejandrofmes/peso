<div class="flex items-center p-2">
    <input type="checkbox" id="checkbox-{{ $value }}" value="{{ $value }}"
        class="h-4 w-4 text-blue-600 border-gray-300 rounded" />
    <label for="checkbox-{{ $value }}"
        class="ml-2 text-sm font-medium text-gray-700 cursor-pointer">{{ $label }}</label>
</div>
